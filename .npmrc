# npm 설치 속도 개선 설정

# 더 빠른 레지스트리 사용 (한국 사용자의 경우)
# registry=https://registry.npmmirror.com/

# 병렬 설치 활성화 (기본값보다 빠름)
prefer-offline=true
fetch-retries=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 불필요한 스크립트 스킵 (선택사항)
# ignore-scripts=true

# 로그 레벨 조정 (warn으로 하면 덜 시끄러움)
loglevel=warn
