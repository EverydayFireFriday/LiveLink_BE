name: "Error"
description: "버그 또는 에러 리포트"
labels: ["bug", "에러"]
assignees: []
body:
  - type: dropdown
    id: priority
    attributes:
      label: "우선순위"
      options:
        - "🔴 Critical"
        - "🟠 High"
        - "🟡 Medium"
        - "🟢 Low"
    validations:
      required: true

  - type: textarea
    id: bug_description
    attributes:
      label: "무슨 문제인가요?"
      placeholder: "예: 로그인 시 500 에러 발생"
    validations:
      required: true

  - type: textarea
    id: steps_to_reproduce
    attributes:
      label: "재현 방법"
      placeholder: |
        1. ~을 클릭
        2. ~을 입력
        3. 에러 발생

  - type: textarea
    id: error_logs
    attributes:
      label: "에러 로그 (선택)"
      placeholder: "에러 메시지나 스크린샷을 첨부해주세요"
      render: shell